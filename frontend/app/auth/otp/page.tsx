import { OtpForm } from "@/components/features/auth/OtpForm";

/**
 * Email verification screen — UI only for now, structured for the future OTP
 * API integration.
 */
export default function OtpPage() {
  return <OtpForm />;
}
